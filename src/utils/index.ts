import { checkFormat, numberOperation } from './Number';
import { Time } from './Time';
import NumberTool from './Number';

export const Number = NumberTool;

export { Time, checkFormat, numberOperation };

export default {
  Time,
  checkFormat,
  numberOperation,
};
